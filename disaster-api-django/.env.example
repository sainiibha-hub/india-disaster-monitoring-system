# Use the SAME values you already used for the Flask/Python monitoring
# system's .env -- this project reads from the same database, it does not
# create a new one.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Ibha@123
DB_NAME=india_disaster_monitoring

DJANGO_SECRET_KEY=change-this-to-something-random
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
DJANGO_TIME_ZONE=Asia/Kolkata
